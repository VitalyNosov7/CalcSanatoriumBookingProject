<Window x:Class="WPFCSB.Views.Windows.AccommodationTypeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPFCSB.Views.Windows"
        mc:Ignorable="d"
        Title="AccommodationTypeWindow" Height="450" Width="800">
	<DockPanel LastChildFill="True" Margin="5">

		<!--<Grid Name="GridTop" DockPanel.Dock="Top" Background="Coral" Width="Auto" Height="30">
	<StackPanel Orientation="Horizontal">
		<TextBlock Margin="3,3,3,3" >Поиск</TextBlock>

		<TextBox Margin="3,3,3,3" 
		HorizontalAlignment="Left" 
		Width="200" 
		Height="20" ></TextBox>

		<Button Margin="3,3,3,3">Найти</Button>
	</StackPanel>
</Grid>-->

		<Grid Name="GridBottom" DockPanel.Dock="Bottom" Background="BlanchedAlmond" Width="Auto" Height="30">
			<StackPanel Orientation="Horizontal">
				<Button Margin="3,3,3,3" Command="{Binding AddAccommodationTypeFromDBCommand}">Добавить</Button>
				<Button Margin="3,3,3,3" Command ="{Binding EditAccommodationTypeToDBCommand}">Редактировать</Button>
				<Button Margin="3,3,3,3" Command="{Binding DeleteAccommodationTypeToDBCommand}">Удалить</Button>
				<Button Margin="3,3,3,3" Command="{Binding DeselectAccommodationTypeCommand}">Снять выделение</Button>
				<Button Margin="3,3,3,3" Command="{Binding UpdateDataAccommodationTypeFromDBCommand}">Обновить данные</Button>
			</StackPanel>
		</Grid>

		<StackPanel Orientation="Horizontal">

			<Grid Width="250">
				<StackPanel Orientation="Vertical">

					<TextBlock>AccommodationTypeID</TextBlock>
					<TextBox Margin="3,3,3,3" 
							HorizontalAlignment="Left" 
							Width="50" 
							Height="20" 
							Text="{Binding AccommodationTypeID}"
							IsEnabled="False">
					</TextBox>

					<TextBlock>SanatoriumID</TextBlock>
					<TextBox Margin="3,3,3,3" 
							HorizontalAlignment="Left" 
							Width="50" 
							Height="20" 
							Text="{Binding SanatoriumID}"
							IsEnabled="False">
					</TextBox>

					<TextBlock>SanatoriumName</TextBlock>
					<StackPanel Orientation="Horizontal">
						<ComboBox x:Name="Sanatorium"
												  Width="200"
												  Margin="3,0,3,1"
												  ItemsSource="{Binding SanatoriumList}"
												  SelectedItem="{Binding SelectedSanatorium, UpdateSourceTrigger=PropertyChanged}"  
												  IsTextSearchEnabled="True"                                            
												  TextSearch.TextPath="SanatoriumName">
							<!--DisplayMemberPath="Name"-->
							<ComboBox.ItemTemplate>
								<DataTemplate>
									<TextBlock>
										<TextBlock.Text>
											<MultiBinding StringFormat="{}{0}">
												<Binding Path="SanatoriumName"/>
											</MultiBinding>
										</TextBlock.Text>
									</TextBlock>
								</DataTemplate>
							</ComboBox.ItemTemplate>
						</ComboBox>
						<Button Content="DB"
												Command="{Binding OpenWindowsCommands.OpenSanatoriumWindomCommand}"
												Width="30"
												Height="20"
												Margin="3,3,3,3"/>
					</StackPanel>

					<TextBlock>RoomCategoryName</TextBlock>
					<TextBox Name="TextTemplateVariabName"
							Margin="3,3,3,3" 
							HorizontalAlignment="Left" 
							Width="240" 
							Height="50" 
							Text="{Binding AccommodationTypeName}">
					</TextBox>


				</StackPanel>
			</Grid>

			<Grid>
				<StackPanel Width="Auto"
					Orientation="Vertical">
					<DataGrid ItemsSource="{Binding AccommodationTypes}" 
					  SelectedItem="{Binding SelectedAccommodationType, Mode=TwoWay}"
					  AutoGenerateColumns="False"
					  GridLinesVisibility="All"
					  CanUserAddRows="False"
					  IsReadOnly="True"
					  Height="400">
						<DataGrid.Columns>
							<DataGridTextColumn Header="ATID" Binding="{Binding AccommodationTypeID}" Width="50"/>
							<DataGridTextColumn Header="SanID" Binding="{Binding SanatoriumID}" Width="50"/>
							<DataGridTextColumn Header="Sanatorium" Binding="{Binding CurrentSanatorium.SanatoriumName}" Width="100"/>
							<DataGridTextColumn Header="ATName" Binding="{Binding AccommodationTypeName}" Width="300"/>
						</DataGrid.Columns>
					</DataGrid>
				</StackPanel>
			</Grid>
		</StackPanel>
	</DockPanel>
</Window>
